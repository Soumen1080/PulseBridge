{
  // Extends base TypeScript configuration from a local file
  // "extends": "./tsconfig.base.json",

  "compilerOptions": {
    // Set the JavaScript language version for emitted code
    "target": "es2021"
  },

  // Exclude the coverage directory from compilation
  "exclude": [
    "coverage"
  ]
}